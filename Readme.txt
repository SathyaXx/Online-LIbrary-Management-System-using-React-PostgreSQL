This project was build using Front End - React js and Back End - Postgresql  and Middleware - Java Servlets.

In DB_Code , the Java class are mentioned in the file itself.

If not mentioned , then it is Java Servlet. Many of it are Java Servlets .

ScreenShots have been attached.

Download necessary packages in package.json using 'npm i' command.

If dependency conflicts, copy the packages used and delete the package.json

Create a new one using npm init on the file and install the libraries.

I have not uploaded node modules, it will be automatically created when you install packages.

Bye guysss.
